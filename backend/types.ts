export type Album = {
    title: string,
    category: string,
    artist: string,
    genre: string,
    release_year: number,
    n_tracks: number,
    rating: number,
}

export type Cinema = {
    title: string, 
    category: string,
    directory: string,
    release_year: number,
    genre: string,
    rating: number,
    durata: number,
}

export type book = {
    title: string,
    category: string,
    author: string,
    release_year: number,
    genre: string,
    pages: number,
    rating: number
}